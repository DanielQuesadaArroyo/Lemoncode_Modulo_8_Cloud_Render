import * as React from "react";
import { AppLayout } from "#layouts";
import { LocationDetailContainer } from "#pods/location-detail";

export const LocationDetailScene = () => (
  <AppLayout>
    <LocationDetailContainer />
  </AppLayout>
);
