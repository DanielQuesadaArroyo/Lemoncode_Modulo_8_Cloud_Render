import * as React from "react";
import { AppLayout } from "#layouts";
import { EpisodeDetailContainer } from "#pods/episode-detail";

export const EpisodeDetailScene = () => (
  <AppLayout>
    <EpisodeDetailContainer />
  </AppLayout>
);
